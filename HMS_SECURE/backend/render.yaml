services:
  - type: web
    name: hms-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DB_NAME
        value: hms_db
      - key: JWT_SECRET
        sync: false
      - key: FRONTEND_URL
        sync: false
